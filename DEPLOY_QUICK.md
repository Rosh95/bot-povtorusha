# ⚡ Быстрый деплой за 5 минут

## Вариант 1: VPS с PM2 (Рекомендуется) ⭐

### Шаг 1: Подключитесь к серверу
```bash
ssh user@your-server-ip
```

### Шаг 2: Установите Node.js
```bash
curl -fsSL https://deb.nodesource.com/setup_18.x | sudo -E bash -
sudo apt install -y nodejs
```

### Шаг 3: Установите PM2
```bash
sudo npm install -g pm2
```

### Шаг 4: Загрузите файлы проекта
```bash
# Через Git
git clone <ваш-репозиторий> bot-povtorusha
cd bot-povtorusha

# Или через SCP с локального компьютера:
# scp -r . user@server:~/bot-povtorusha
```

### Шаг 5: Настройте
```bash
npm install
nano .env  # Добавьте BOT_TOKEN=ваш_токен
```

### Шаг 6: Запустите
```bash
pm2 start ecosystem.config.js
pm2 save
pm2 startup  # Следуйте инструкциям
```

Готово! Бот работает 24/7 🎉

---

## Вариант 2: Railway.app (Самый простой)

1. Зайдите на [railway.app](https://railway.app)
2. Подключите GitHub репозиторий
3. Добавьте переменные:
   - `BOT_TOKEN` = ваш токен
   - `TIMEZONE` = Europe/Moscow
4. Готово! Railway задеплоит автоматически

---

## Вариант 3: Amvera.ru (Российская платформа) 🇷🇺

1. Зарегистрируйтесь на [amvera.ru](https://amvera.ru/)
2. Создайте проект → Node.js приложение
3. Подключите Git репозиторий (или используйте выделенный от Amvera)
4. Добавьте переменные окружения:
   - `BOT_TOKEN` = ваш токен
   - `TIMEZONE` = Europe/Moscow
5. Сделайте push: `git push amvera main`
6. Готово! Бот задеплоится автоматически

**Преимущества**: Российская платформа, бесплатный стартовый баланс, автоматический деплой

## Вариант 4: Render.com

1. Зайдите на [render.com](https://render.com)
2. New → Background Worker
3. Подключите репозиторий
4. Настройки:
   - Build: `npm install`
   - Start: `node index.js`
5. Добавьте переменные окружения
6. Deploy

---

## Проверка работы

```bash
# На VPS
pm2 logs bot-povtorusha

# Проверьте в Telegram - отправьте /start боту
```

---

## Обновление бота

```bash
# На VPS
cd ~/bot-povtorusha
git pull
npm install
pm2 restart bot-povtorusha
```

---

Подробная инструкция: [docs/DEPLOYMENT.md](docs/DEPLOYMENT.md)
