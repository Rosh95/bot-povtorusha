# ВопросДня от Повторюши 🤖

Telegram-бот для подготовки к ЕГЭ, который ежедневно отправляет вопросы по выбранному предмету.

## Возможности

- 📚 Поддержка нескольких предметов (математика, русский язык, физика, обществознание, история)
- ⏰ Настраиваемое время уведомлений
- 📊 Статистика ответов
- 💡 Подробные объяснения к вопросам
- ⏰ Напоминания о неотвеченных вопросах

## Установка

1. Клонируйте репозиторий или скачайте файлы

2. Установите зависимости:
```bash
npm install
```

3. Создайте файл `.env` на основе `.env.example`:
```bash
cp .env.example .env
```

4. Получите токен бота у [@BotFather](https://t.me/BotFather) и добавьте его в `.env`:
```
BOT_TOKEN=your_bot_token_here
```

5. Запустите бота:
```bash
npm start
```

Для разработки с автоперезагрузкой:
```bash
npm run dev
```

## Структура проекта

```
bot-povtorusha/
├── index.js                 # Основной файл бота
├── config.js                # Конфигурация
├── package.json             # Зависимости
├── database/
│   └── db.js               # Работа с базой данных SQLite
├── handlers/
│   ├── commandHandlers.js  # Обработчики команд
│   └── questionHandler.js  # Логика работы с вопросами
├── utils/
│   └── scheduler.js        # Планировщик задач (cron)
└── questions/
    └── math.json           # Вопросы по математике (пример)
```

## Команды бота

- `/start` - Регистрация и приветствие
- `/help` - Список команд
- `/subject` - Выбрать предмет
- `/time` - Изменить время уведомлений
- `/today` - Получить вопрос прямо сейчас
- `/progress` - Статистика ответов
- `/stop` - Приостановить уведомления
- `/resume` - Возобновить уведомления

## Добавление вопросов

### Быстрый способ (рекомендуется)

Используйте интерактивный скрипт:

```bash
npm run add-question
```

Скрипт проведет вас через все шаги добавления вопроса.

### Валидация вопросов

Перед добавлением вопросов проверьте их валидность:

```bash
# Проверить все файлы
npm run validate

# Проверить конкретный предмет
npm run validate math
```

### Подробная документация

Смотрите полное руководство: [docs/ADDING_QUESTIONS.md](docs/ADDING_QUESTIONS.md)

### Формат вопроса

Вопросы хранятся в формате JSON в папке `questions/`. Каждый файл соответствует предмету (например, `math.json`, `russian.json`).

Пример тестового вопроса:

```json
{
  "id": "math_1",
  "topic": "Алгебра - Квадратные уравнения",
  "type": "test",
  "question": "Решите уравнение: x² - 5x + 6 = 0",
  "options": ["x = 2, x = 3", "x = -2, x = -3", "x = 1, x = 6", "x = -1, x = -6"],
  "correctAnswer": 0,
  "answer": "x = 2, x = 3",
  "explanation": "Используем формулу дискриминанта..."
}
```

Шаблон для новых вопросов: `questions/TEMPLATE.json`

## База данных

Бот использует SQLite для хранения:
- Информации о пользователях
- Статистики ответов
- Истории отправленных вопросов

База данных создается автоматически при первом запуске в папке `database/bot.db`.

## Логирование

Ошибки записываются в файл `errors.log` в корне проекта.

## Безопасность

⚠️ **Важно**: `BOT_TOKEN` - это секрет! Никогда не публикуйте его в Git или открытых репозиториях.

- ✅ Файл `.env` уже добавлен в `.gitignore`
- ✅ Используйте `.env.example` как шаблон (без реальных токенов)
- 📖 Подробнее: [docs/SECURITY.md](docs/SECURITY.md)

## Деплой

Подробное руководство по деплою бота на различные платформы: [docs/DEPLOYMENT.md](docs/DEPLOYMENT.md)

### Быстрый деплой на VPS

```bash
# 1. Настройка сервера
./scripts/setup-vps.sh

# 2. Загрузите файлы проекта

# 3. Создайте .env файл с BOT_TOKEN

# 4. Деплой
./scripts/deploy.sh
```

### Деплой на Amvera.ru (Российская платформа) 🇷🇺

1. Зарегистрируйтесь на [amvera.ru](https://amvera.ru/)
2. Создайте проект → Node.js приложение
3. Подключите Git репозиторий
4. Добавьте переменные окружения: `BOT_TOKEN`, `TIMEZONE`
5. Сделайте push: `git push amvera main`
6. Готово! Деплой автоматический

### Деплой на Railway/Render

1. Подключите GitHub репозиторий
2. Добавьте переменные окружения: `BOT_TOKEN`, `TIMEZONE`
3. Деплой произойдет автоматически

## Лицензия

ISC
