# 📝 Руководство по добавлению вопросов

## Быстрый старт

### Способ 1: Интерактивный скрипт (рекомендуется)

Самый простой способ - использовать интерактивный скрипт:

```bash
node scripts/add-question.js
```

Скрипт проведет вас через все шаги:
1. Выбор предмета
2. Ввод данных вопроса
3. Предпросмотр
4. Сохранение

### Способ 2: Ручное редактирование JSON

1. Откройте файл `questions/[предмет].json` (например, `questions/math.json`)
2. Добавьте новый вопрос в массив
3. Проверьте валидность: `node scripts/validate-questions.js [предмет]`

## Формат вопроса

### Тестовый вопрос (type: "test")

```json
{
  "id": "math_1",
  "topic": "Алгебра - Квадратные уравнения",
  "type": "test",
  "question": "Решите уравнение: x² - 5x + 6 = 0",
  "options": [
    "x = 2, x = 3",
    "x = -2, x = -3",
    "x = 1, x = 6",
    "x = -1, x = -6"
  ],
  "correctAnswer": 0,
  "answer": "x = 2, x = 3",
  "explanation": "Используем формулу дискриминанта: D = b² - 4ac = 25 - 24 = 1. Тогда x₁ = (5 + 1)/2 = 3, x₂ = (5 - 1)/2 = 2."
}
```

### Открытый вопрос (type: "open")

```json
{
  "id": "russian_1",
  "topic": "Орфография",
  "type": "open",
  "question": "В каком слове пишется буква 'о'?",
  "answer": "В слове 'молоко'",
  "explanation": "Проверочное слово - 'молок'"
}
```

## Поля вопроса

### Обязательные поля

- **id** (string) - Уникальный идентификатор вопроса (например, "math_1", "russian_5")
- **type** (string) - Тип вопроса: "test" или "open"
- **question** (string) - Текст вопроса

### Для тестовых вопросов (type: "test")

- **options** (array) - Массив вариантов ответов (минимум 2, максимум 6)
- **correctAnswer** (number) - Индекс правильного ответа (начинается с 0)

### Опциональные поля

- **topic** (string) - Тема вопроса (отображается как заголовок)
- **answer** (string) - Краткий ответ
- **explanation** (string) - Подробное объяснение решения

## Правила именования ID

- Формат: `[предмет]_[номер]`
- Примеры: `math_1`, `russian_5`, `physics_12`
- ID должны быть уникальными в рамках одного файла

## Валидация

Перед добавлением вопросов в продакшн, проверьте их валидность:

```bash
# Проверить все файлы
node scripts/validate-questions.js

# Проверить конкретный предмет
node scripts/validate-questions.js math
```

## Примеры вопросов

Смотрите файлы в папке `questions/`:
- `math.json` - примеры вопросов по математике
- `TEMPLATE.json` - шаблон для новых вопросов

## Массовое добавление

Если у вас много вопросов, вы можете:

1. Создать временный JSON файл с вопросами
2. Использовать скрипт для объединения:

```javascript
// Пример скрипта для объединения
import { readFileSync, writeFileSync } from 'fs';

const newQuestions = JSON.parse(readFileSync('new-questions.json', 'utf-8'));
const existingQuestions = JSON.parse(readFileSync('questions/math.json', 'utf-8'));

const merged = [...existingQuestions, ...newQuestions];
writeFileSync('questions/math.json', JSON.stringify(merged, null, 2));
```

3. Проверить валидность: `node scripts/validate-questions.js math`

## Советы

1. **Используйте понятные ID** - это поможет при отладке
2. **Добавляйте объяснения** - они помогают пользователям учиться
3. **Проверяйте валидность** - перед коммитом всегда запускайте валидатор
4. **Следите за уникальностью** - не дублируйте ID вопросов
5. **Группируйте по темам** - используйте поле `topic` для организации

## Структура папки questions

```
questions/
├── math.json          # Вопросы по математике
├── russian.json       # Вопросы по русскому языку
├── physics.json      # Вопросы по физике
├── social.json       # Вопросы по обществознанию
├── history.json      # Вопросы по истории
└── TEMPLATE.json     # Шаблон для новых вопросов
```

## Добавление нового предмета

1. Создайте файл `questions/[название].json` (например, `chemistry.json`)
2. Добавьте предмет в `config.js`:
   ```javascript
   subjects: {
     // ... существующие предметы
     chemistry: 'Химия'
   }
   ```
3. Добавьте несколько вопросов в новый файл
4. Проверьте валидность: `node scripts/validate-questions.js chemistry`
5. Перезапустите бота
