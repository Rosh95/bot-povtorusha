# 🚀 Руководство по деплою бота

## Варианты деплоя

### 1. VPS (Рекомендуется для начала) ⭐
### 2. Railway.app (Простой облачный деплой)
### 3. Render.com (Бесплатный хостинг)
### 4. Heroku (Классический вариант)
### 5. Docker (Для продвинутых)

---

## Вариант 1: VPS (VDS) с PM2

### Требования
- VPS с Ubuntu/Debian (например, от Timeweb, Selectel, DigitalOcean)
- Минимум 512MB RAM, 1 CPU
- SSH доступ

### Шаг 1: Подготовка сервера

```bash
# Обновление системы
sudo apt update && sudo apt upgrade -y

# Установка Node.js (версия 18+)
curl -fsSL https://deb.nodesource.com/setup_18.x | sudo -E bash -
sudo apt install -y nodejs

# Проверка версии
node -v
npm -v

# Установка Git
sudo apt install -y git
```

### Шаг 2: Клонирование проекта

```bash
# Переходим в домашнюю директорию
cd ~

# Клонируем репозиторий (или загружаем файлы через SCP/SFTP)
git clone <ваш-репозиторий> bot-povtorusha
# ИЛИ создаем директорию и загружаем файлы вручную

cd bot-povtorusha
```

### Шаг 3: Установка зависимостей

```bash
npm install
```

### Шаг 4: Настройка переменных окружения

```bash
# Создаем .env файл
nano .env
```

Добавьте:
```
BOT_TOKEN=ваш_токен_бота
TIMEZONE=Europe/Moscow
```

Сохраните: `Ctrl+X`, затем `Y`, затем `Enter`

### Шаг 5: Установка PM2 (менеджер процессов)

```bash
# Установка PM2 глобально
sudo npm install -g pm2

# Запуск бота через PM2
pm2 start index.js --name "bot-povtorusha"

# Сохранение конфигурации PM2
pm2 save

# Настройка автозапуска при перезагрузке
pm2 startup
# Выполните команду, которую выведет PM2 (обычно что-то вроде):
# sudo env PATH=$PATH:/usr/bin pm2 startup systemd -u ваш_пользователь --hp /home/ваш_пользователь
```

### Шаг 6: Полезные команды PM2

```bash
# Просмотр статуса
pm2 status

# Просмотр логов
pm2 logs bot-povtorusha

# Перезапуск
pm2 restart bot-povtorusha

# Остановка
pm2 stop bot-povtorusha

# Удаление из PM2
pm2 delete bot-povtorusha
```

### Шаг 7: Настройка файрвола (если нужно)

```bash
# Обычно для бота файрвол не нужен, но если есть:
sudo ufw allow 22/tcp  # SSH
sudo ufw enable
```

---

## Вариант 2: Railway.app

### Преимущества
- ✅ Бесплатный тариф (с ограничениями)
- ✅ Автоматический деплой из Git
- ✅ Простая настройка

### Шаг 1: Подготовка

1. Зарегистрируйтесь на [railway.app](https://railway.app)
2. Подключите GitHub/GitLab репозиторий

### Шаг 2: Создание проекта

1. Нажмите "New Project"
2. Выберите "Deploy from GitHub repo"
3. Выберите ваш репозиторий

### Шаг 3: Настройка переменных окружения

1. Перейдите в Settings → Variables
2. Добавьте:
   - `BOT_TOKEN` = ваш токен бота
   - `TIMEZONE` = Europe/Moscow

### Шаг 4: Настройка деплоя

Railway автоматически определит Node.js проект. Убедитесь, что в `package.json` есть:

```json
{
  "scripts": {
    "start": "node index.js"
  }
}
```

### Шаг 5: Деплой

Railway автоматически задеплоит при каждом push в репозиторий.

---

## Вариант 3: Render.com

### Преимущества
- ✅ Бесплатный тариф
- ✅ Автоматический деплой

### Шаг 1: Регистрация

1. Зарегистрируйтесь на [render.com](https://render.com)
2. Подключите GitHub

### Шаг 2: Создание Web Service

1. Нажмите "New" → "Web Service"
2. Подключите репозиторий
3. Настройки:
   - **Name**: bot-povtorusha
   - **Environment**: Node
   - **Build Command**: `npm install`
   - **Start Command**: `node index.js`

### Шаг 3: Переменные окружения

В разделе "Environment":
- `BOT_TOKEN` = ваш токен
- `TIMEZONE` = Europe/Moscow

### Шаг 4: Деплой

Нажмите "Create Web Service" - Render задеплоит автоматически.

**Важно**: Render может "засыпать" бесплатные сервисы. Для бота лучше использовать Background Worker вместо Web Service.

---

## Вариант 4: Heroku

### Шаг 1: Установка Heroku CLI

```bash
# Linux
curl https://cli-assets.heroku.com/install.sh | sh

# Или через npm
npm install -g heroku
```

### Шаг 2: Создание Procfile

Создайте файл `Procfile` в корне проекта:

```
worker: node index.js
```

### Шаг 3: Деплой

```bash
# Логин
heroku login

# Создание приложения
heroku create bot-povtorusha

# Установка переменных окружения
heroku config:set BOT_TOKEN=ваш_токен
heroku config:set TIMEZONE=Europe/Moscow

# Деплой
git push heroku main

# Просмотр логов
heroku logs --tail
```

---

## Вариант 5: Amvera.ru (Российская платформа) 🇷🇺

### Преимущества
- ✅ Российская платформа
- ✅ Бесплатный стартовый баланс при регистрации
- ✅ Автоматический деплой через Git
- ✅ Поддержка Node.js из коробки
- ✅ Простая настройка

### Шаг 1: Регистрация

1. Зарегистрируйтесь на [amvera.ru](https://amvera.ru/)
2. Подтвердите email
3. Стартовый баланс активируется автоматически

### Шаг 2: Создание проекта

1. В личном кабинете нажмите "Создать проект"
2. Выберите тип: "Node.js приложение"
3. Назовите проект: `bot-povtorusha`

### Шаг 3: Подключение репозитория

**Вариант А: Выделенный репозиторий Amvera**

1. Amvera создаст Git репозиторий автоматически
2. Добавьте remote:
   ```bash
   git remote add amvera <url-репозитория-amvera>
   ```

**Вариант Б: Свой репозиторий**

1. В настройках проекта подключите свой GitHub/GitLab репозиторий
2. Укажите ветку (обычно `main` или `master`)

### Шаг 4: Создание конфигурационного файла

Создайте файл `amvera.yaml` в корне проекта (уже создан):

```yaml
name: bot-povtorusha
runtime: nodejs18
entrypoint: node index.js

resources:
  memory: 512Mi
  cpu: 0.5

restartPolicy: always
```

### Шаг 5: Настройка переменных окружения

В интерфейсе Amvera перейдите в настройки проекта → "Переменные окружения":

- `BOT_TOKEN` = ваш токен Telegram бота
- `TIMEZONE` = Europe/Moscow (опционально)

### Шаг 6: Деплой

**Если используете Git репозиторий Amvera:**

```bash
# Добавьте файлы
git add .
git commit -m "Initial deploy"
git push amvera main
```

**Если подключен свой репозиторий:**

Просто сделайте push в вашу ветку:
```bash
git push origin main
```

Amvera автоматически обнаружит изменения и задеплоит проект.

### Шаг 7: Проверка работы

1. В интерфейсе Amvera перейдите в "Логи"
2. Проверьте, что бот запустился без ошибок
3. Отправьте `/start` боту в Telegram

### Обновление бота

Просто сделайте push в репозиторий:

```bash
git add .
git commit -m "Update bot"
git push amvera main  # или git push origin main
```

Amvera автоматически обновит приложение.

### Полезные команды

- **Логи**: Просматривайте в интерфейсе Amvera → "Логи"
- **Перезапуск**: В интерфейсе → "Перезапустить"
- **Мониторинг**: Статистика использования в личном кабинете

### Особенности Amvera

- ✅ Автоматическое масштабирование при росте нагрузки
- ✅ Резервирование данных в 3 экземплярах
- ✅ Ежедневные бэкапы
- ✅ Автоматическое восстановление при сбоях (10-15 минут)
- ✅ Поддержка API OpenAI через иностранный IP (бесплатно)

### Тарифы

- Есть бесплатный стартовый баланс при регистрации
- Подробнее о тарифах: [amvera.ru/tariffs](https://amvera.ru/tariffs)

---

## Вариант 6: Docker

### Создание Dockerfile

Создайте файл `Dockerfile`:

```dockerfile
FROM node:18-alpine

WORKDIR /app

# Копируем package файлы
COPY package*.json ./

# Устанавливаем зависимости
RUN npm ci --only=production

# Копируем остальные файлы
COPY . .

# Создаем директории для БД и логов
RUN mkdir -p database questions

# Запуск
CMD ["node", "index.js"]
```

### Создание .dockerignore

```
node_modules
npm-debug.log
.env
*.db
*.log
.git
```

### Запуск Docker контейнера

```bash
# Сборка образа
docker build -t bot-povtorusha .

# Запуск контейнера
docker run -d \
  --name bot-povtorusha \
  --restart unless-stopped \
  -e BOT_TOKEN=ваш_токен \
  -e TIMEZONE=Europe/Moscow \
  -v $(pwd)/database:/app/database \
  -v $(pwd)/questions:/app/questions \
  bot-povtorusha

# Просмотр логов
docker logs -f bot-povtorusha
```

---

## Общие рекомендации

### 1. Безопасность

- ✅ Никогда не коммитьте `.env` файл
- ✅ Используйте сильные пароли для SSH
- ✅ Регулярно обновляйте зависимости: `npm audit fix`

### 2. Мониторинг

- Настройте уведомления об ошибках (можно добавить отправку в Telegram)
- Регулярно проверяйте логи
- Мониторьте использование ресурсов

### 3. Резервное копирование

```bash
# Бэкап базы данных
cp database/bot.db database/bot.db.backup

# Или автоматический бэкап (cron)
0 2 * * * cp /path/to/bot/database/bot.db /path/to/backup/bot-$(date +\%Y\%m\%d).db
```

### 4. Обновление бота

```bash
# На VPS с PM2
cd ~/bot-povtorusha
git pull
npm install
pm2 restart bot-povtorusha

# На Railway/Render - просто push в репозиторий
```

---

## Проверка работы

После деплоя проверьте:

1. ✅ Бот отвечает на команды
2. ✅ Вопросы отправляются по расписанию
3. ✅ Логи не содержат ошибок
4. ✅ База данных создается и работает

---

## Решение проблем

### Бот не запускается

```bash
# Проверьте логи
pm2 logs bot-povtorusha
# или
docker logs bot-povtorusha

# Проверьте переменные окружения
echo $BOT_TOKEN
```

### Ошибки с базой данных

```bash
# Проверьте права доступа
ls -la database/

# Пересоздайте БД (если нужно)
rm database/bot.db
# Бот создаст новую при запуске
```

### Проблемы с зависимостями

```bash
# Очистка и переустановка
rm -rf node_modules package-lock.json
npm install
```

---

## Рекомендация для начинающих

**Начните с VPS + PM2** - это самый простой и надежный вариант:
- Полный контроль
- Простая настройка
- Бесплатно (если есть свой VPS)
- Легко отлаживать
