# 🔒 Безопасность и секреты

## Важно! Переменные окружения могут содержать секреты

### Секретные переменные (НЕ публикуйте!)

- **`BOT_TOKEN`** - токен Telegram бота
  - ⚠️ **КРИТИЧЕСКИ ВАЖНО**: Если кто-то узнает ваш токен, он сможет управлять вашим ботом!
  - Никогда не коммитьте в Git
  - Не публикуйте в открытых репозиториях
  - Не делитесь в чатах/форумах

### Публичные переменные (можно публиковать)

- **`TIMEZONE`** - часовой пояс (например, `Europe/Moscow`)
  - Это просто настройка, не секрет
  - Можно оставить в коде или в `.env.example`

---

## Как правильно хранить секреты

### ✅ Правильно

1. **Локально (разработка)**
   ```bash
   # Создайте .env файл (уже в .gitignore)
   echo "BOT_TOKEN=ваш_токен" > .env
   ```

2. **На сервере (VPS)**
   ```bash
   # Создайте .env на сервере
   nano .env
   # Добавьте BOT_TOKEN=ваш_токен
   ```

3. **В облачных платформах (Amvera, Railway, Render)**
   - Используйте интерфейс для добавления переменных окружения
   - Они хранятся безопасно и не видны в коде

### ❌ НЕПРАВИЛЬНО

```javascript
// ❌ НИКОГДА ТАК НЕ ДЕЛАЙТЕ!
const BOT_TOKEN = "1234567890:ABCdefGHIjklMNOpqrsTUVwxyz";
```

```bash
# ❌ НЕ коммитьте .env в Git
git add .env  # НЕ ДЕЛАЙТЕ ЭТО!
```

```bash
# ❌ НЕ публикуйте токен в открытом репозитории
# Даже если потом удалите - он останется в истории Git
```

---

## Проверка безопасности

### 1. Убедитесь, что .env в .gitignore

Проверьте файл `.gitignore`:
```
.env
.env.local
.env.production
```

### 2. Проверьте, что .env не в Git

```bash
# Проверка, не отслеживается ли .env
git ls-files | grep .env

# Если ничего не вывело - хорошо!
# Если вывело .env - удалите из Git:
git rm --cached .env
```

### 3. Проверьте историю Git

Если случайно закоммитили токен:

```bash
# 1. Немедленно отзовите токен в @BotFather
# 2. Создайте новый токен
# 3. Очистите историю Git (если репозиторий приватный)
# 4. Если репозиторий публичный - токен уже скомпрометирован
```

---

## Что делать, если токен утек?

1. **Немедленно отзовите токен**
   - Зайдите к [@BotFather](https://t.me/BotFather)
   - `/revoke` → выберите бота
   - Создайте новый токен

2. **Обновите токен везде**
   - Локальный `.env`
   - Сервер (VPS)
   - Облачные платформы (Amvera, Railway и т.д.)

3. **Проверьте активность бота**
   - Убедитесь, что никто не использовал старый токен

---

## Безопасное хранение на разных платформах

### Amvera.ru

1. В интерфейсе проекта → "Переменные окружения"
2. Добавьте `BOT_TOKEN` = ваш токен
3. ✅ Токен хранится зашифрованным и не виден в коде

### Railway.app

1. Settings → Variables
2. Add Variable: `BOT_TOKEN`
3. ✅ Безопасное хранение

### Render.com

1. Environment → Environment Variables
2. Add: `BOT_TOKEN`
3. ✅ Защищено

### VPS (локальный сервер)

```bash
# Создайте .env файл
nano .env

# Добавьте (файл уже в .gitignore)
BOT_TOKEN=ваш_токен

# Установите правильные права (только владелец может читать)
chmod 600 .env
```

---

## Рекомендации

1. ✅ Используйте `.env.example` для примера (без реальных токенов)
2. ✅ Всегда проверяйте `.gitignore` перед коммитом
3. ✅ Используйте разные токены для разработки и продакшена
4. ✅ Регулярно ротируйте токены (меняйте раз в несколько месяцев)
5. ✅ Не храните токены в скриншотах/сообщениях
6. ✅ Используйте менеджеры паролей для хранения токенов

---

## Пример безопасной структуры

```
bot-povtorusha/
├── .env              # СЕКРЕТ - в .gitignore ✅
├── .env.example     # ПУБЛИЧНЫЙ - пример без токенов ✅
├── .gitignore       # Игнорирует .env ✅
└── config.js        # Читает из process.env ✅
```

---

## Дополнительные меры безопасности

### Для продакшена

1. Используйте отдельный токен для продакшена
2. Ограничьте доступ к серверу/платформе
3. Включите двухфакторную аутентификацию где возможно
4. Регулярно проверяйте логи на подозрительную активность
5. Делайте бэкапы базы данных

### Мониторинг

- Следите за необычной активностью бота
- Проверяйте количество пользователей
- Отслеживайте ошибки в логах

---

## Итог

- 🔴 **BOT_TOKEN** = СЕКРЕТ (никогда не публикуйте!)
- 🟢 **TIMEZONE** = не секрет (можно публиковать)
- ✅ `.env` уже в `.gitignore` - безопасно
- ✅ Используйте `.env.example` для примеров
- ✅ Храните секреты в интерфейсах платформ, а не в коде
